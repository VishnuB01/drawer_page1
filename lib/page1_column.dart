import 'package:flutter/material.dart';

class Page1Column extends StatelessWidget {
  const Page1Column({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisAlignment: MainAxisAlignment.center,
      
      children: [
        
        
       Center(
        child: Text("Demo Drawer"),
      
       )
      ],
     
    );
  }
}
